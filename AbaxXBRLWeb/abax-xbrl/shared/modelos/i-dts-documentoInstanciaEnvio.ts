module abaxXBRL.shared.modelos {
    /** 
    * Contrato que define un elemento de entrada de la taxonomia.
    **/
    export interface IDtsDocumentoInstanciaEnviado {
        /** Referencia como punto de entrada de la taxonomia*/
        Href: string;
    }
}